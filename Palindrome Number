class Solution {
public:
    bool isPalindrome(int x) {
        string p=to_string(x);
        for(int i=0;i<p.length()/2;i++){
            if(p[i]!=p[p.length()-i-1]){
                return false;
            }
        }
        return true;
    }
};
