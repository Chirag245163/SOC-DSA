class Solution {
public:
    int minOperations(vector<int>& nums, int k) {
        int sum_1=0;
        for(int i=0;i<nums.size();i++){
            sum_1=sum_1+nums[i];
        }
        int z=sum_1%k;
        return z;
    }
};
